name: offline-audit-lock

on:
push:
branches:
- main
pull_request:

jobs:
offline-audit-lock:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v4
- name: Run offline audit lock
shell: bash
run: bash scripts/offline-audit-lock.sh
