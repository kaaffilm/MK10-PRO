# MK10-PRO v1.0.3 Audit Receipt

The audit receipt is the machine-readable output of the v1.0.3 external audit packet.

It is generated by:

    bash scripts/audit-receipt-proof.sh

The generated receipt is not a new claim surface.

It records that the existing public proof chain still passes:

- public surface proof
- public replay perimeter
- auditor replay entrypoint
- external audit packet

The receipt remains valid only while:

- VERSION is 1.0.3
- PACKAGE_SURFACES.json is locked to 1.0.3
- packages/npm/package.json is locked to 1.0.3
- PyPI remains the canonical runtime witness
- NPM and PKG remain non-canonical public package surfaces
- no public package version is raised
