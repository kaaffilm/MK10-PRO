
# @kaaffilm/mk10-pro

Public NPM package surface for MK10-PRO.

Version: `1.0.3`

## Boundary

This NPM package is a registry discovery surface.

It does not reimplement MK10-PRO.

It does not expand MK10-PRO's claim boundary.

It is not the canonical runtime witness.

## Canonical runtime witness

Use PyPI:

```bash
pip install mk10-pro==1.0.3
mk10 proof
```

## NPM registry proof

```bash
npm view @kaaffilm/mk10-pro@1.0.3 version
```

## No-version-raise rule

Do not raise the public package version to repair an immutable registry artifact.

MK10-PRO public package surfaces are locked at `1.0.3`.
